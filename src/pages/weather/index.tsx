import React from 'react';
import WeatherComponent from "../../components/weather";

const Weather = () => {
    return (
        <div>
            <h1>Weather page</h1>
            <WeatherComponent/>
        </div>
    );
};

export default Weather;